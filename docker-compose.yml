services:
  backend:
    build:
      context: ./backend
    container_name: network-monitor-backend
    env_file:
      - ./backend/.env.example
    ports:
      - "8000:8000"
    restart: unless-stopped
